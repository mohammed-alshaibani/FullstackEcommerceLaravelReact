# متجر إلكتروني (Laravel + React)

## نظرة عامة

مشروع متجر إلكتروني يجمع بين Laravel في الخلفية وReact.js في الواجهة الأمامية مع نظام مصادقة JWT ودمج Stripe للدفع.

## المميزات الرئيسية

### 🚀 التقنيات المستخدمة
- **PHP 7.1.3+** - لغة البرمجة الخلفية
- **Laravel** - إطار العمل الخلفي
- **React.js** - مكتبة الواجهة الأمامية
- **JWT Auth** - نظام المصادقة
- **Stripe** - بوابة الدفع الإلكتروني
- **MySQL** - قاعدة البيانات

### 📦 هيكل المشروع
```
Ecommerce-master/
├── app/                    # منطق التطبيق Laravel
│   ├── Http/              # وحدات التحكم والوسائط
│   ├── Models/            # نماذج قاعدة البيانات
│   └── Providers/         # مزودي الخدمات
├── resources/
│   └── assets/
│       └── js/
│           └── components/ # مكونات React
│           └── Main.js    # المسارات الرئيسية
├── config/                # إعدادات Laravel
├── database/              # قواعد البيانات والترحيل
├── public/                # الملفات العامة
├── routes/                # مسارات API والويب
└── storage/               # ملفات التخزين
```

### 🛍️ مميزات المتجر
- **واجهة مستخدم تفاعلية** باستخدام React
- **نظام مصادقة آمن** بـ JWT
- **دفع إلكتروني** عبر Stripe
- **سلة تسوق ديناميكية**
- **إدارة المنتجات**
- **نظام الطلبات**
- **البحث والفلترة**

### 🔧 متطلبات التشغيل
- PHP >= 7.1.3
- Composer
- Node.js & NPM
- MySQL
- Web Server

### 📋 خطوات التثبيت
1. تثبيت الاعتماديات:
   ```bash
   composer install
   npm install
   ```

2. إنشاء مفتاح التطبيق:
   ```bash
   php artisan key:generate
   ```

3. إعداد JWT:
   ```bash
   php artisan jwt:secret
   ```

4. تشغيل المشروع:
   ```bash
   npm run dev
   php artisan serve
   ```

